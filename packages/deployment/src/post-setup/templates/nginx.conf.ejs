server {
    listen 80;

    # hosts.api - api.my-domain.com
    server_name <%= hosts.api %>;

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header Host $http_host;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_pass http://localhost:<%= ports.api %>/;
      proxy_redirect off;
      proxy_read_timeout 240s;
    }
}

server {
    listen 80;

    # hosts.panel - my-domain.com
    server_name <%= hosts.panel %>;

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header Host $http_host;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_pass http://localhost:<%= ports.panel %>/;
      proxy_redirect off;
      proxy_read_timeout 240s;
    }
}

server {
    listen 80;

    # hosts.components - components.my-domain.com
    server_name <%= hosts.components %>;

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header Host $http_host;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_pass http://localhost:<%= ports.components %>/;
      proxy_redirect off;
      proxy_read_timeout 240s;
    }
}
